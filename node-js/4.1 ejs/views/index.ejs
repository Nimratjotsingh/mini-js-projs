<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=<device-width>, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1><%=title%></h1>
    <p>Current Seconds: <%= seconds %></p>
    <ul>
        <% if(seconds % 2 ===0){items.forEach(element => { %>
            <li> <%=element%></li> 
        <%});} else{ %>
            <p> No itmes to display</p>
       <% }%>
    </ul>
    <%-htmlContent%>
    <%-include("footer.ejs")%>
</body>
</html>